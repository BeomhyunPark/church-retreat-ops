spring:
  flyway:
    enabled: true
    locations: classpath:db/migration

mybatis:
  mapper-locations: classpath*:mapper/**/*.xml
  configuration:
    map-underscore-to-camel-case: true

app:
  check-in:
    qr-expires-at: 2099-12-31T23:59:59+09:00
