<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gmc.retreat.checkin.mapper.CheckInMapper">
    <resultMap id="CheckInRosterItemResultMap" type="com.gmc.retreat.checkin.domain.CheckInRosterItem">
        <constructor>
            <arg column="participant_id" javaType="java.lang.Long" />
            <arg column="name" javaType="java.lang.String" />
            <arg column="gender" javaType="com.gmc.retreat.registration.domain.Gender" />
            <arg column="birth_year" javaType="java.lang.Integer" />
            <arg column="phone_last_four" javaType="java.lang.String" />
            <arg column="church_cell_id" javaType="java.lang.Long" />
            <arg column="church_cell_name" javaType="java.lang.String" />
            <arg column="middle_group_id" javaType="java.lang.Long" />
            <arg column="middle_group_name" javaType="java.lang.String" />
            <arg column="retreat_group_id" javaType="java.lang.Long" />
            <arg column="retreat_group_name" javaType="java.lang.String" />
            <arg column="retreat_group_leader" javaType="java.lang.Boolean" />
            <arg column="checked_in" javaType="java.lang.Boolean" />
            <arg column="checked_in_at" javaType="java.time.OffsetDateTime" />
            <arg column="check_in_method" javaType="com.gmc.retreat.checkin.domain.CheckInMethod" />
            <arg column="checked_in_by_admin_id" javaType="java.lang.Long" />
            <arg column="checked_in_by_admin_name" javaType="java.lang.String" />
            <arg column="cancelled_at" javaType="java.time.OffsetDateTime" />
            <arg column="cancelled_by_admin_id" javaType="java.lang.Long" />
            <arg column="cancelled_by_admin_name" javaType="java.lang.String" />
        </constructor>
    </resultMap>

    <sql id="RosterColumns">
        r.id AS participant_id, r.name, r.gender, r.birth_year, r.phone_last_four,
        r.church_cell_id, cc.name AS church_cell_name,
        mg.id AS middle_group_id, mg.name AS middle_group_name,
        rg.id AS retreat_group_id, rg.name AS retreat_group_name, rgm.leader AS retreat_group_leader,
        COALESCE(ci.checked_in, FALSE) AS checked_in, ci.checked_in_at, ci.check_in_method,
        ci.checked_in_by_admin_id, checked_admin.name AS checked_in_by_admin_name,
        ci.cancelled_at, ci.cancelled_by_admin_id, cancelled_admin.name AS cancelled_by_admin_name
    </sql>

    <sql id="RosterJoins">
        FROM registrations r
        LEFT JOIN church_cells cc ON cc.id = r.church_cell_id
        LEFT JOIN church_middle_groups mg ON mg.id = cc.church_middle_group_id
        LEFT JOIN retreat_group_members rgm ON rgm.registration_id = r.id
        LEFT JOIN retreat_groups rg ON rg.id = rgm.retreat_group_id
        LEFT JOIN retreat_check_ins ci ON ci.participant_id = r.id
        LEFT JOIN admin_users checked_admin ON checked_admin.id = ci.checked_in_by_admin_id
        LEFT JOIN admin_users cancelled_admin ON cancelled_admin.id = ci.cancelled_by_admin_id
    </sql>

    <sql id="RosterFilters">
        <where>
            AND r.retreat_id = (
                SELECT id FROM retreats WHERE status IN ('DRAFT', 'OPEN')
            )
            <if test="checkedIn != null">
                AND COALESCE(ci.checked_in, FALSE) = #{checkedIn}
            </if>
            <if test="retreatGroupId != null">
                AND rg.id = #{retreatGroupId}
            </if>
            <if test="churchCellId != null">
                AND r.church_cell_id = #{churchCellId}
            </if>
            <if test="keyword != null and keyword != ''">
                AND (
                    lower(r.name) LIKE lower(concat('%', #{keyword}, '%'))
                    OR r.phone_last_four = #{keyword}
                )
            </if>
        </where>
    </sql>

    <select id="findRoster" resultMap="CheckInRosterItemResultMap">
        SELECT
        <include refid="RosterColumns" />
        <include refid="RosterJoins" />
        <include refid="RosterFilters" />
        ORDER BY COALESCE(ci.checked_in, FALSE) ASC, rg.display_order ASC NULLS LAST,
                 rg.name ASC NULLS LAST, r.name ASC, r.id ASC
        LIMIT #{limit}
        OFFSET #{offset}
    </select>

    <select id="countRoster" resultType="long">
        SELECT COUNT(*)
        <include refid="RosterJoins" />
        <include refid="RosterFilters" />
    </select>

    <select id="findRosterItemByParticipantId" resultMap="CheckInRosterItemResultMap">
        SELECT
        <include refid="RosterColumns" />
        <include refid="RosterJoins" />
        WHERE r.id = #{participantId}
          AND r.retreat_id = (
              SELECT id FROM retreats WHERE status IN ('DRAFT', 'OPEN')
          )
    </select>

    <select id="countParticipantById" resultType="int">
        SELECT COUNT(*)
        FROM registrations
        WHERE id = #{participantId}
          AND retreat_id = (
              SELECT id FROM retreats WHERE status IN ('DRAFT', 'OPEN')
          )
    </select>

    <select id="upsertCheckInIfNotCheckedIn" resultType="java.lang.Long">
        INSERT INTO retreat_check_ins (
            participant_id, checked_in, checked_in_at, checked_in_by_admin_id, check_in_method
        )
        VALUES (
            #{participantId}, TRUE, now(), #{adminId}, #{method}
        )
        ON CONFLICT (participant_id) DO UPDATE
        SET checked_in = TRUE,
            checked_in_at = now(),
            checked_in_by_admin_id = EXCLUDED.checked_in_by_admin_id,
            check_in_method = EXCLUDED.check_in_method,
            cancelled_at = NULL,
            cancelled_by_admin_id = NULL,
            cancellation_reason = NULL,
            updated_at = now()
        WHERE retreat_check_ins.checked_in = FALSE
        RETURNING id
    </select>

    <select id="cancelCheckInIfCheckedIn" resultType="com.gmc.retreat.checkin.domain.CheckInMethod">
        UPDATE retreat_check_ins
        SET checked_in = FALSE,
            cancelled_at = now(),
            cancelled_by_admin_id = #{adminId},
            cancellation_reason = #{reason},
            updated_at = now()
        WHERE participant_id = #{participantId}
          AND checked_in = TRUE
        RETURNING check_in_method
    </select>

    <insert id="insertEvent" parameterType="com.gmc.retreat.checkin.mapper.CheckInEventInsert">
        INSERT INTO retreat_check_in_events (
            participant_id, action, method, performed_by_admin_id, reason
        )
        VALUES (
            #{participantId}, #{action}, #{method}, #{adminId}, #{reason}
        )
    </insert>

    <insert id="insertToken" parameterType="com.gmc.retreat.checkin.mapper.CheckInTokenInsert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO participant_check_in_tokens (
            participant_id, token_hash, expires_at, issued_by_admin_id
        )
        VALUES (
            #{participantId}, #{tokenHash}, #{expiresAt}, #{adminId}
        )
    </insert>

    <update id="revokeActiveTokensByParticipantId">
        UPDATE participant_check_in_tokens
        SET revoked_at = now()
        WHERE participant_id = #{participantId}
          AND revoked_at IS NULL
          AND expires_at > now()
    </update>

    <select id="findLatestRevokedAtByParticipantId" resultType="java.time.OffsetDateTime">
        SELECT revoked_at
        FROM participant_check_in_tokens
        WHERE participant_id = #{participantId}
          AND revoked_at IS NOT NULL
        ORDER BY revoked_at DESC
        LIMIT 1
    </select>
</mapper>
