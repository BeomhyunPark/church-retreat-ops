<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gmc.retreat.fee.mapper.FeeMapper">
    <resultMap id="FeeRosterItemResultMap" type="com.gmc.retreat.fee.domain.FeeRosterItem">
        <constructor>
            <arg column="participant_id" javaType="java.lang.Long" />
            <arg column="name" javaType="java.lang.String" />
            <arg column="gender" javaType="com.gmc.retreat.registration.domain.Gender" />
            <arg column="birth_year" javaType="java.lang.Integer" />
            <arg column="phone_last_four" javaType="java.lang.String" />
            <arg column="church_cell_id" javaType="java.lang.Long" />
            <arg column="church_cell_name" javaType="java.lang.String" />
            <arg column="middle_group_id" javaType="java.lang.Long" />
            <arg column="middle_group_name" javaType="java.lang.String" />
            <arg column="retreat_group_id" javaType="java.lang.Long" />
            <arg column="retreat_group_name" javaType="java.lang.String" />
            <arg column="retreat_group_leader" javaType="java.lang.Boolean" />
            <arg column="fee_paid" javaType="java.lang.Boolean" />
            <arg column="fee_status_updated_at" javaType="java.time.OffsetDateTime" />
            <arg column="fee_status_updated_by_admin_id" javaType="java.lang.Long" />
            <arg column="fee_status_updated_by_admin_name" javaType="java.lang.String" />
        </constructor>
    </resultMap>

    <resultMap id="FeeEventResultMap" type="com.gmc.retreat.fee.domain.FeeEvent">
        <constructor>
            <arg column="id" javaType="java.lang.Long" />
            <arg column="participant_id" javaType="java.lang.Long" />
            <arg column="previous_fee_paid" javaType="java.lang.Boolean" />
            <arg column="new_fee_paid" javaType="java.lang.Boolean" />
            <arg column="changed_by_admin_id" javaType="java.lang.Long" />
            <arg column="changed_by_admin_name" javaType="java.lang.String" />
            <arg column="reason" javaType="java.lang.String" />
            <arg column="created_at" javaType="java.time.OffsetDateTime" />
        </constructor>
    </resultMap>

    <sql id="FeeColumns">
        r.id AS participant_id, r.name, r.gender, r.birth_year, r.phone_last_four,
        r.church_cell_id, cc.name AS church_cell_name,
        mg.id AS middle_group_id, mg.name AS middle_group_name,
        rg.id AS retreat_group_id, rg.name AS retreat_group_name, rgm.leader AS retreat_group_leader,
        r.fee_paid, r.fee_status_updated_at, r.fee_status_updated_by_admin_id,
        fee_admin.name AS fee_status_updated_by_admin_name
    </sql>

    <sql id="FeeJoins">
        FROM registrations r
        LEFT JOIN church_cells cc ON cc.id = r.church_cell_id
        LEFT JOIN church_middle_groups mg ON mg.id = cc.church_middle_group_id
        LEFT JOIN retreat_group_members rgm ON rgm.registration_id = r.id
        LEFT JOIN retreat_groups rg ON rg.id = rgm.retreat_group_id
        LEFT JOIN admin_users fee_admin ON fee_admin.id = r.fee_status_updated_by_admin_id
    </sql>

    <sql id="RosterFilters">
        <where>
            <if test="feePaid != null">
                r.fee_paid = #{feePaid}
            </if>
            <if test="retreatGroupId != null">
                AND rg.id = #{retreatGroupId}
            </if>
            <if test="churchCellId != null">
                AND r.church_cell_id = #{churchCellId}
            </if>
            <if test="keyword != null and keyword != ''">
                AND (
                    lower(r.name) LIKE lower(concat('%', #{keyword}, '%'))
                    OR r.phone_last_four = #{keyword}
                )
            </if>
        </where>
    </sql>

    <select id="findRoster" resultMap="FeeRosterItemResultMap">
        SELECT
        <include refid="FeeColumns" />
        <include refid="FeeJoins" />
        <include refid="RosterFilters" />
        ORDER BY r.fee_paid ASC, rg.display_order ASC NULLS LAST,
                 rg.name ASC NULLS LAST, r.name ASC, r.id ASC
        LIMIT #{limit}
        OFFSET #{offset}
    </select>

    <select id="countRoster" resultType="long">
        SELECT COUNT(*)
        <include refid="FeeJoins" />
        <include refid="RosterFilters" />
    </select>

    <select id="findRosterItemByParticipantId" resultMap="FeeRosterItemResultMap">
        SELECT
        <include refid="FeeColumns" />
        <include refid="FeeJoins" />
        WHERE r.id = #{participantId}
    </select>

    <select id="countParticipantById" resultType="int">
        SELECT COUNT(*)
        FROM registrations
        WHERE id = #{participantId}
    </select>

    <select id="findFeePaidByParticipantId" resultType="java.lang.Boolean">
        SELECT fee_paid
        FROM registrations
        WHERE id = #{participantId}
    </select>

    <select id="updateFeePaidIfChanged" resultType="java.lang.Boolean">
        WITH target AS (
            SELECT id, fee_paid
            FROM registrations
            WHERE id = #{participantId}
              AND fee_paid &lt;&gt; #{feePaid}
            FOR UPDATE
        ),
        updated AS (
            UPDATE registrations r
            SET fee_paid = #{feePaid},
                fee_status_updated_at = now(),
                fee_status_updated_by_admin_id = #{adminId},
                updated_at = now()
            FROM target
            WHERE r.id = target.id
            RETURNING r.id
        )
        SELECT target.fee_paid
        FROM target
        JOIN updated ON updated.id = target.id
    </select>

    <insert id="insertEvent" parameterType="com.gmc.retreat.fee.mapper.FeeEventInsert">
        INSERT INTO registration_fee_events (
            registration_id, previous_fee_paid, new_fee_paid, changed_by_admin_id, reason
        )
        VALUES (
            #{participantId}, #{previousFeePaid}, #{newFeePaid}, #{adminId}, #{reason}
        )
    </insert>

    <select id="findEventsByParticipantId" resultMap="FeeEventResultMap">
        SELECT e.id, e.registration_id AS participant_id,
               e.previous_fee_paid, e.new_fee_paid,
               e.changed_by_admin_id, a.name AS changed_by_admin_name,
               e.reason, e.created_at
        FROM registration_fee_events e
        JOIN admin_users a ON a.id = e.changed_by_admin_id
        WHERE e.registration_id = #{participantId}
        ORDER BY e.created_at DESC, e.id DESC
        LIMIT #{limit}
    </select>
</mapper>
