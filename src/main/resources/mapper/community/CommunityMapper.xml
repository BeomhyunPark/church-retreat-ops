<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gmc.retreat.community.mapper.CommunityMapper">
    <resultMap id="ChurchMiddleGroupResultMap" type="com.gmc.retreat.community.domain.ChurchMiddleGroup">
        <constructor>
            <arg column="id" javaType="java.lang.Long" />
            <arg column="name" javaType="java.lang.String" />
            <arg column="elder_name" javaType="java.lang.String" />
            <arg column="description" javaType="java.lang.String" />
            <arg column="display_order" javaType="java.lang.Integer" />
            <arg column="active" javaType="java.lang.Boolean" />
            <arg column="created_at" javaType="java.time.OffsetDateTime" />
            <arg column="updated_at" javaType="java.time.OffsetDateTime" />
        </constructor>
    </resultMap>

    <resultMap id="ChurchCellResultMap" type="com.gmc.retreat.community.domain.ChurchCell">
        <constructor>
            <arg column="id" javaType="java.lang.Long" />
            <arg column="church_middle_group_id" javaType="java.lang.Long" />
            <arg column="middle_group_name" javaType="java.lang.String" />
            <arg column="name" javaType="java.lang.String" />
            <arg column="cell_leader_name" javaType="java.lang.String" />
            <arg column="description" javaType="java.lang.String" />
            <arg column="display_order" javaType="java.lang.Integer" />
            <arg column="active" javaType="java.lang.Boolean" />
            <arg column="created_at" javaType="java.time.OffsetDateTime" />
            <arg column="updated_at" javaType="java.time.OffsetDateTime" />
        </constructor>
    </resultMap>

    <sql id="MiddleGroupColumns">
        id, name, elder_name, description, display_order, active, created_at, updated_at
    </sql>

    <sql id="CellColumns">
        c.id, c.church_middle_group_id, mg.name AS middle_group_name, c.name,
        c.cell_leader_name, c.description, c.display_order, c.active,
        c.created_at, c.updated_at
    </sql>

    <select id="findMiddleGroups" resultMap="ChurchMiddleGroupResultMap">
        SELECT
        <include refid="MiddleGroupColumns" />
        FROM church_middle_groups
        ORDER BY display_order ASC, name ASC, id ASC
    </select>

    <select id="findMiddleGroupById" resultMap="ChurchMiddleGroupResultMap">
        SELECT
        <include refid="MiddleGroupColumns" />
        FROM church_middle_groups
        WHERE id = #{id}
    </select>

    <select id="findMiddleGroupByName" resultMap="ChurchMiddleGroupResultMap">
        SELECT
        <include refid="MiddleGroupColumns" />
        FROM church_middle_groups
        WHERE name = #{name}
    </select>

    <insert id="insertMiddleGroup" parameterType="com.gmc.retreat.community.mapper.ChurchMiddleGroupUpsert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO church_middle_groups (name, elder_name, description, display_order)
        VALUES (#{name}, #{elderName}, #{description}, #{displayOrder})
    </insert>

    <update id="updateMiddleGroup" parameterType="com.gmc.retreat.community.mapper.ChurchMiddleGroupUpsert">
        UPDATE church_middle_groups
        SET name = #{name},
            elder_name = #{elderName},
            description = #{description},
            display_order = #{displayOrder},
            updated_at = now()
        WHERE id = #{id}
    </update>

    <update id="updateMiddleGroupActive">
        UPDATE church_middle_groups
        SET active = #{active},
            updated_at = now()
        WHERE id = #{id}
    </update>

    <select id="findCells" resultMap="ChurchCellResultMap">
        SELECT
        <include refid="CellColumns" />
        FROM church_cells c
        JOIN church_middle_groups mg ON mg.id = c.church_middle_group_id
        WHERE (#{middleGroupId}::BIGINT IS NULL OR c.church_middle_group_id = #{middleGroupId})
          AND (#{active}::BOOLEAN IS NULL OR c.active = #{active})
        ORDER BY mg.display_order ASC, mg.name ASC, c.display_order ASC, c.name ASC, c.id ASC
    </select>

    <select id="findCellById" resultMap="ChurchCellResultMap">
        SELECT
        <include refid="CellColumns" />
        FROM church_cells c
        JOIN church_middle_groups mg ON mg.id = c.church_middle_group_id
        WHERE c.id = #{id}
    </select>

    <select id="findCellByMiddleGroupAndName" resultMap="ChurchCellResultMap">
        SELECT
        <include refid="CellColumns" />
        FROM church_cells c
        JOIN church_middle_groups mg ON mg.id = c.church_middle_group_id
        WHERE c.church_middle_group_id = #{middleGroupId}
          AND c.name = #{name}
    </select>

    <insert id="insertCell" parameterType="com.gmc.retreat.community.mapper.ChurchCellUpsert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO church_cells (
            church_middle_group_id, name, cell_leader_name, description, display_order
        )
        VALUES (
            #{middleGroupId}, #{name}, #{cellLeaderName}, #{description}, #{displayOrder}
        )
    </insert>

    <update id="updateCell" parameterType="com.gmc.retreat.community.mapper.ChurchCellUpsert">
        UPDATE church_cells
        SET church_middle_group_id = #{middleGroupId},
            name = #{name},
            cell_leader_name = #{cellLeaderName},
            description = #{description},
            display_order = #{displayOrder},
            updated_at = now()
        WHERE id = #{id}
    </update>

    <update id="updateCellActive">
        UPDATE church_cells
        SET active = #{active},
            updated_at = now()
        WHERE id = #{id}
    </update>
</mapper>
